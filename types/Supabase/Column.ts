export type Competitions = {
  id: string,
  title: string,
  poster: string,
  description: string,
  deadline: Date,
}

export type IdeaBoxTags = {
  id: number,
  tagId: number
}

export type Tags = {
  id: number,
  name: string
}